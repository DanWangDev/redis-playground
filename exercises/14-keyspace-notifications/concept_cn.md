# 练习 14：键空间通知

## 你将学到
- 使用 `CONFIG SET notify-keyspace-events` 启用键空间事件
- 使用 `PSUBSCRIBE` 订阅键事件
- 检测键的创建、删除和过期
- 理解传递保证：即发即忘

## 为什么重要
键空间通知允许你的应用实时响应数据变化——缓存失效、会话过期清理、审计日志。

## 常见陷阱
- 通知是即发即忘的——如果订阅者离线，事件会丢失。
- 高事件量可能使 Pub/Sub 通道饱和。
- `notify-keyspace-events` 默认禁用——必须显式配置。
