# 练习 18：分布式锁

## 你将学到
- 使用 `SET NX PX` 获取锁
- 使用 Lua 脚本安全释放锁
- 理解 Redlock 算法
- 使用栅栏令牌防止锁饥饿

## 为什么重要
分布式锁协调多个进程/服务器之间对共享资源的访问。Redis 提供简单但强大的锁原语。然而，分布式锁定出人意料地微妙。

## 常见陷阱
- 绝不在不验证令牌的情况下释放锁——使用 Lua 脚本。
- 始终设置 TTL——防止锁持有者崩溃时死锁。
- Redlock 有争议（Martin Kleppmann 的批评）——理解其局限性。
