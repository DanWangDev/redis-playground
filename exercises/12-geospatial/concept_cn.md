# 练习 12：地理位置

## 你将学到
- 使用 `GEOADD` 存储坐标
- 使用 `GEORADIUS` / `GEORADIUSBYMEMBER` 按半径查询
- 使用 `GEODIST` 计算距离
- 使用 `GEOPOS` 获取坐标

## 为什么重要
Redis 地理空间索引存储经纬度对并提供半径查询——查找 5km 内的所有餐厅、定位最近的司机、计算距离。基于有序集合构建，操作复杂度 O(log N)。

## 常见陷阱
- 坐标顺序是**经度，纬度**（不是纬度，经度）。
- 有效范围：经度 -180..180，纬度 -85..85。
